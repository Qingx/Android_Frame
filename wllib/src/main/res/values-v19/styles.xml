<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/main_status_color</item>
        <item name="colorPrimary">@color/main_status_color</item>
        <item name="colorPrimaryDark">@color/md_grey_200</item>

        <item name="android:windowBackground">@drawable/draw_main_background</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">false</item>
    </style>

    <!--    创建一个透明的主题-->
    <style name="AppTheme.Transparent">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowAnimationStyle">@android:style/Animation</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <!-- 开启沉浸式状态栏 -->
    <style name="AppTheme.Immersion">
        <item name="colorPrimaryDark">#22000000</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">false</item>
    </style>

</resources>
